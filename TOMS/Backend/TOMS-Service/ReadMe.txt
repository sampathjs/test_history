If you are working from within the JM Network do the following:

- Copy mavenrc_pre.cmd to %USERPROFILE%

This command is going to ensure that maven uses the keystore trust.jks to gain access to the repository providing the node / npm packages that are necessary for the build